# TCP-SQLite-Intranet
A C application for educational porpouse that implement a comunication system (between a client and a server) based on TCP protocol that permits the execution of CRUD operation on SQLite DB
